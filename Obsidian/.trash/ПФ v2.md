Функция СведенияОВнешнейОбработке() Экспорт
	
	ПараметрыРегистрации = ДополнительныеОтчетыИОбработки.СведенияОВнешнейОбработке("2.2.2.1");
	ПараметрыРегистрации.Назначение.Добавить("Документ.ПриемНаРаботу");
	ПараметрыРегистрации.Информация = НСтр("ru = 'Трудовой договор ИП Попов И.А.'");
	ПараметрыРегистрации.Вид = ДополнительныеОтчетыИОбработкиКлиентСервер.ВидОбработкиПечатнаяФорма();
	ПараметрыРегистрации.Версия = "1.4";	
	ПараметрыРегистрации.БезопасныйРежим = Ложь;
	
	Команда = ПараметрыРегистрации.Команды.Добавить();
	Команда.Представление = НСтр("ru = 'Трудовой договор ИП Попов И.А. (MS WORD)'");
	Команда.Идентификатор = "ПостоянныйТрудовойДоговор";
	Команда.Использование = ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода();
	Команда.ПоказыватьОповещение = Истина;
	
	Возврат ПараметрыРегистрации;
	
КонецФункции    

Процедура Печать(МассивОбъектов, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода) Экспорт
	
	ПечатнаяФорма = УправлениеПечатью.СведенияОПечатнойФорме(КоллекцияПечатныхФорм, "ПостоянныйТрудовойДоговор");
	
	Если ПечатнаяФорма <> Неопределено Тогда
		ПечатнаяФорма.ОфисныеДокументы = ПолучитьОфисныеДокументы(МассивОбъектов, ПараметрыВывода);
	КонецЕсли
	
КонецПроцедуры

Функция ПолучитьОфисныеДокументы(МассивОбъектов) Экспорт
	
	ОфисныеДокументы = Новый Соответствие;
	
	Для Каждого СсылкаНаОбъект Из МассивОбъектов Цикл
		
		ДанныеОбъекта = ЗаполнениеПечатнойФормы(СсылкаНаОбъект);
		Стр0 = ДанныеОбъекта.ТелоДокумента[0];         
		
		НаименованиеФайла = "Трудовой договор № " + Стр0.НомерДоговора + " от " + Стр0.ДатаДоговора;
		
		ОфисныеДокументы.Вставить(
			СформироватьПечатнуюФорму(ДанныеОбъекта), 
			НаименованиеФайла);	                    
			 
	КонецЦикла;	
	
	Возврат ОфисныеДокументы;
	
КонецФункции

Функция ЗаполнениеПечатнойФормы(СсылкаНаДокумент) Экспорт

	УстановитьПривилегированныйРежим(Истина);

	ДанныеОбъекта = Новый Структура();

	Если СсылкаНаДокумент = Неопределено Тогда
		Возврат ДанныеОбъекта;
	КонецЕсли;

	ОбъектПриема = СсылкаНаДокумент.ПолучитьОбъект();
	ДатаСреза = ОбъектПриема.Дата;

	ПоказательРайонныйКоэффициент = НайтиПоказательВДокументе(ОбъектПриема, "Районный коэффициент");
	//Если ПоказательРайонныйКоэффициент = Неопределено Тогда
		//ВызватьИсключение "В документе 'Прием на работу' не найден показатель 'Районный коэффициент' (ТЧ Показатели).";
	//КонецЕсли;

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ПриемНаРаботу.Ссылка КАК Ссылка,
	|	ПриемНаРаботу.Номер КАК НомерДоговора,
	|	ПриемНаРаботу.Дата КАК ДатаДоговора,
	|	ПриемНаРаботу.Сотрудник.Наименование КАК ФИОСотрудника,
	|	ПриемНаРаботу.Должность.Наименование КАК ДолжностьСотрудника,
	|	ПриемНаРаботу.СовокупнаяТарифнаяСтавка КАК ТарифнаяСтавка,
	|	ПриемНаРаботу.Сотрудник.ФизическоеЛицо.ИНН КАК ИННСотрудника,
	|	ПриемНаРаботу.Сотрудник.ФизическоеЛицо.СтраховойНомерПФР КАК СНИЛССотрудника,
	|	ПриемНаРаботу.Сотрудник КАК Сотрудник,
	|
	|	ДокументыФизическихЛиц.Серия КАК СерияСотрудник,
	|	ДокументыФизическихЛиц.Номер КАК НомерСотрудник,
	|	ДокументыФизическихЛиц.КемВыдан КАК КемВыдан,
	|	ДокументыФизическихЛиц.ДатаВыдачи КАК ДатаВыдачи,
	|	ДокументыФизическихЛиц.КодПодразделения КАК КодПодразделения
	|ПОМЕСТИТЬ ВТШапка
	|ИЗ
	|	Документ.ПриемНаРаботу КАК ПриемНаРаботу
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДокументыФизическихЛиц.СрезПоследних(&ДатаСреза) КАК ДокументыФизическихЛиц
	|		ПО ПриемНаРаботу.Сотрудник.ФизическоеЛицо = ДокументыФизическихЛиц.Физлицо
	|ГДЕ
	|	ПриемНаРаботу.Ссылка = &Ссылка
	|;
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	&Ссылка КАК Ссылка,
	|	СУММА(ВЫБОР
	|			КОГДА ПриемНаРаботуПоказатели.Показатель = &РайонныйКоэффициент
	|				ТОГДА ПриемНаРаботуПоказатели.Значение
	|			ИНАЧЕ 0
	|		КОНЕЦ) КАК РайонныйКоэффициент
	|ПОМЕСТИТЬ ВТПоказателиОплаты
	|ИЗ
	|	Документ.ПриемНаРаботу.Показатели КАК ПриемНаРаботуПоказатели
	|ГДЕ
	|	ПриемНаРаботуПоказатели.Ссылка = &Ссылка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Шапка.НомерДоговора,
	|	Шапка.ДатаДоговора,
	|	Шапка.ФИОСотрудника,
	|	Шапка.ДолжностьСотрудника,
	|	Шапка.ТарифнаяСтавка,
	|	Шапка.ИННСотрудника,
	|	Шапка.СНИЛССотрудника,
	|	Шапка.Сотрудник КАК Сотрудник,
	|	Шапка.СерияСотрудник КАК СерияПаспортаСотрудника,
	|	Шапка.НомерСотрудник КАК НомерПаспортаСотрудника,
	|	Шапка.КемВыдан КАК КемВыданПаспортСотрудника,
	|	Шапка.ДатаВыдачи КАК ДатаВыдачиПаспортаСотрудника,
	|	Шапка.КодПодразделения КАК КодПодразделенияПаспортаСотрудника,
	|	ЕСТЬNULL(Показатели.РайонныйКоэффициент, 0) КАК РайонныйКоэффициент
	|ИЗ
	|	ВТШапка КАК Шапка
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТПоказателиОплаты КАК Показатели
	|		ПО Показатели.Ссылка = Шапка.Ссылка
	|;";

	Запрос.УстановитьПараметр("Ссылка", СсылкаНаДокумент);
	Запрос.УстановитьПараметр("ДатаСреза", ДатаСреза);
	Запрос.УстановитьПараметр("РайонныйКоэффициент", ПоказательРайонныйКоэффициент);

	Выборка = Запрос.Выполнить().Выбрать();
	Если НЕ Выборка.Следующий() Тогда
		Возврат ДанныеОбъекта;
	КонецЕсли;
	
	АдресСотрудника = "";

	ДанныеСотр = КадровыйУчет.КадровыеДанныеСотрудников(
	Истина,
	ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Выборка.Сотрудник),
	"АдресМестаПроживанияПредставление, АдресПоПропискеПредставление",
	Выборка.ДатаДоговора
	);

	Если ДанныеСотр.Количество() > 0 Тогда
		АдресСотрудника = ДанныеСотр[0].АдресМестаПроживанияПредставление;
		Если ПустаяСтрока(АдресСотрудника) Тогда
			АдресСотрудника = ДанныеСотр[0].АдресПоПропискеПредставление;
		КонецЕсли;
	КонецЕсли;

	МассивТело = Новый Массив;
	СтрТело = Новый Структура;

	СтрТело.Вставить("НомерДоговора", Выборка.НомерДоговора);
	СтрТело.Вставить("ДатаДоговора", Формат(Выборка.ДатаДоговора, "ДФ=dd.MM.yyyy"));

	СтрТело.Вставить("ФИОСотрудника", Выборка.ФИОСотрудника);
	СтрТело.Вставить("ДолжностьСотрудника", Выборка.ДолжностьСотрудника);
	СтрТело.Вставить("АдресСотрудника", АдресСотрудника);

	СтрТело.Вставить("СуммаОклада", Формат(Выборка.ТарифнаяСтавка, "ЧГ=0; ЧРД=."));
	СтрТело.Вставить("СуммаОкладаПрописью",
		СокрЛП(ЧислоПрописью(Выборка.ТарифнаяСтавка, "Л=ru_RU", НСтр("ru=', , , м, , , , ж, 0'")))
	);

	СтрТело.Вставить("РайонныйКоэффициент", Строка(Выборка.РайонныйКоэффициент));

	СтрТело.Вставить("ИННСотрудника", Выборка.ИННСотрудника);
	СтрТело.Вставить("СНИЛССотрудника", Выборка.СНИЛССотрудника);

	СтрТело.Вставить("СерияПаспортаСотрудника", Выборка.СерияПаспортаСотрудника);
	СтрТело.Вставить("НомерПаспортаСотрудника", Выборка.НомерПаспортаСотрудника);
	СтрТело.Вставить("КемВыданПаспортСотрудника", Выборка.КемВыданПаспортСотрудника);
	СтрТело.Вставить("КодПодразделенияПаспортаСотрудника", Выборка.КодПодразделенияПаспортаСотрудника);

	Если ЗначениеЗаполнено(Выборка.ДатаВыдачиПаспортаСотрудника) Тогда
		СтрТело.Вставить("ДатаВыдачиПаспортаСотрудника", Формат(Выборка.ДатаВыдачиПаспортаСотрудника, "ДФ=dd.MM.yyyy"));
	Иначе
		СтрТело.Вставить("ДатаВыдачиПаспортаСотрудника", "");
	КонецЕсли;

	МассивТело.Добавить(СтрТело);
	ДанныеОбъекта.Вставить("ТелоДокумента", МассивТело);

	Возврат ДанныеОбъекта;
КонецФункции

Функция НайтиПоказательВДокументе(ОбъектПриема, НаименованиеПоказателя) Экспорт

	Если ОбъектПриема = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Если НЕ ОбъектПриема.Метаданные().ТабличныеЧасти.Найти("Показатели") <> Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Для Каждого СтрП Из ОбъектПриема.Показатели Цикл
		Если СтрП.Показатель <> Неопределено И СтрП.Показатель.Наименование = НаименованиеПоказателя Тогда
			Возврат СтрП.Показатель;
		КонецЕсли;
	КонецЦикла;

	Возврат Неопределено;

КонецФункции 

Функция СформироватьПечатнуюФорму (ДанныеОбъекта)
	МакетДокумента = ПолучитьМакет("ПФ_DOC_ТрудовойДоговорПопов");
	Макет = УправлениеПечатью.ИнициализироватьМакетОфисногоДокумента(МакетДокумента, Неопределено, Неопределено);
	
	ОписаниеОбластей = Новый Структура;  
	УправлениеПечатью.ДобавитьОписаниеОбласти(ОписаниеОбластей, "Заголовок", "Общая");
	УправлениеПечатью.ДобавитьОписаниеОбласти(ОписаниеОбластей, "Основа", "Общая");
	УправлениеПечатью.ДобавитьОписаниеОбласти(ОписаниеОбластей, "Подвал", "Общая");
	
	ПечатнаяФорма = УправлениеПечатью.ИнициализироватьПечатнуюФорму(ПараметрыВывода, Неопределено, Макет);
	
	Область = УправлениеПечатью.ОбластьМакета(Макет, ОписаниеОбластей["Заголовок"]);
	УправлениеПечатью.ПрисоединитьОбластьИЗаполнитьПараметры(ПечатнаяФорма, Область, ДанныеОбъекта);
	
	Область = УправлениеПечатью.ОбластьМакета(Макет, ОписаниеОбластей["Основа"]);
	УправлениеПечатью.ПрисоединитьОбластьИЗаполнитьПараметры(ПечатнаяФорма, Область, ДанныеОбъекта);
	
	Область = УправлениеПечатью.ОбластьМакета(Макет, ОписаниеОбластей["Подвал"]);
	УправлениеПечатью.ПрисоединитьОбластьИЗаполнитьПараметры(ПечатнаяФорма, Область, ДанныеОбъекта);
	
	Адрес = УправлениеПечатью.СформироватьДокумент(ПечатнаяФорма);
	
	УправлениеПечатью.ОчиститьСсылки(ПечатнаяФорма);
	УправлениеПечатью.ОчиститьСсылки(Макет);
	
	Возврат Адрес;
КонецФункции	
	
Функция КоличествоДнейСтрокой(Знач Дней)
	
	Если Дней >= 5 И Дней <= 20 Тогда
		Возврат Строка(Дней) + НСтр("ru = ' дней'");	
	ИначеЕсли Дней % 10 = 1 Тогда	
		Возврат Строка(Дней) + НСтр("ru = ' день'");	
	ИначеЕсли Дней % 10 = 2 Тогда	
		Возврат Строка(Дней) + НСтр("ru = ' дня'");	
	Иначе
		Возврат Строка(Дней) + НСтр("ru = ' дней'");	
	КонецЕсли;
	
КонецФункции


